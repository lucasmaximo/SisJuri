@model Sisjuri.Models.processo

@{
    ViewBag.Title = "Editar processo";
}

<div class="row">
    <div class="main-box">
        <h2>Editar processo</h2>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
                <hr />
                @Html.ValidationSummary(true)
                @Html.HiddenFor(model => model.id_processo)

                <div class="form-group">
                    @Html.LabelFor(model => model.num_processo, "Número do processo", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(model => model.num_processo, new { size = 25, maxlength = 25 })
                        @Html.ValidationMessageFor(model => model.num_processo)
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.descricao_processo, "Descrição", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.TextAreaFor(model => model.descricao_processo, new { cols = 50, rows = 4 })
                        @Html.ValidationMessageFor(model => model.descricao_processo)
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.fk_id_juri, "Pertence ao", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.DropDownListFor(model => model.fk_id_juri, new SelectList(ViewData["lstJuri"] as System.Collections.IEnumerable, "id_juri", "nome_juri"))
                        @Html.ValidationMessageFor(model => model.fk_id_juri)
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Salvar" class="btn btn-default" />
                    </div>
                </div>
            </div>
        }
        <div>
            @Html.ActionLink("Cancelar", "List")
        </div>
    </div>
</div>